interface GameState {
    board: [[String!]]
    turn: ID!
}

type InitState implements GameState {
    board: [[String!]]
    turn: ID!
}

type StartState implements GameState {
    board: [[String!]]
    turn: ID!
}

type TurnState implements GameState {
    board: [[String!]]
    turn: ID!
}

type EndState implements GameState {
    board: [[String!]]
    turn: ID!
}
