interface GameEvent {
    id: ID!
}

type JoinGameEvent implements GameEvent {
    id: ID!
    playerId: ID!
}

type MarkEvent implements GameEvent {
    id: ID!
    symbol: String!
    x: Int!
    y: Int!
}
